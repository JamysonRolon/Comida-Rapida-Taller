# Diseño previo a la implementación

## Arquitectura

```mermaid
flowchart TD
    FX[JavaFX: formularios y tablas] --> C[controller: coordinación de vistas]
    C --> S[service: permisos, validación, carrito y ventas]
    S --> R[repository: Spring Data JPA]
    R --> E[model: entidades Hibernate]
    E --> DB[(PostgreSQL local)]
    CFG[config: Spring Boot sin servidor web] --> S
```

Paquetes: `config`, `controller`, `model`, `repository`, `service`, `exception`, `util`. Sin controladores HTTP. El controlador presenta formularios, recoge entradas y delega. Un ejecutor de tareas ejecuta los servicios fuera del hilo JavaFX. Los resultados regresan al hilo gráfico. El contexto Spring se inicia al abrir la aplicación y se cierra al salir.

## Modelo entidad-relación

```mermaid
erDiagram
    CATEGORIA ||--o{ PRODUCTO : clasifica
    CLIENTE o|--o{ VENTA : realiza
    USUARIO ||--o{ VENTA : registra
    VENTA ||--|{ DETALLE_VENTA : contiene
    PRODUCTO ||--o{ DETALLE_VENTA : vendido
    USUARIO {
        bigint id PK
        varchar nombre
        varchar username UK
        varchar password
        varchar rol
        boolean activo
    }
    CLIENTE {
        bigint id PK
        varchar tipo_documento
        varchar numero_documento UK
        varchar nombre
        varchar apellido
        varchar telefono
        varchar correo
        boolean activo
    }
    CATEGORIA {
        bigint id PK
        varchar nombre UK
        boolean activo
    }
    PRODUCTO {
        bigint id PK
        varchar nombre
        varchar descripcion
        numeric precio
        integer stock
        boolean activo
        bigint categoria_id FK
    }
    VENTA {
        bigint id PK
        timestamptz fecha_hora
        numeric total
        bigint cliente_id FK
        bigint usuario_id FK
    }
    DETALLE_VENTA {
        bigint id PK
        bigint venta_id FK
        bigint producto_id FK
        integer cantidad
        numeric precio_unitario
        numeric subtotal
    }
```

Se crean exclusivamente las seis tablas solicitadas. IDs `BIGINT GENERATED BY DEFAULT AS IDENTITY`. Dinero `NUMERIC(14,2)` y `BigDecimal`; total/subtotal > 0, cantidad > 0, precio > 0, stock >= 0. Claves foráneas conservan el historial. Índices en fecha de venta y claves foráneas. Índice único en `lower(nombre)` para categorías. El detalle no repite un producto dentro de la misma venta.

La fecha se persiste como instante con zona y se consulta por intervalo [inicio del día, inicio del día siguiente) calculado para America/Bogota. El nombre actual de un cliente/producto se utiliza al reabrir comprobantes; los importes se leen del detalle histórico. No se añade almacenamiento de copias de nombres, porque no lo exige el modelo solicitado.

## Diagrama de clases principales

```mermaid
classDiagram
    class Usuario {
        -Long id
        -String nombre
        -String username
        -String password
        -Rol rol
        -boolean activo
    }
    class Cliente {
        -Long id
        -String tipoDocumento
        -String numeroDocumento
        -String nombre
        -String apellido
        -String telefono
        -String correo
        -boolean activo
    }
    class Categoria {
        -Long id
        -String nombre
        -boolean activo
    }
    class Producto {
        -Long id
        -String nombre
        -String descripcion
        -BigDecimal precio
        -int stock
        -boolean activo
        +actualizar()
        +descontar(int cantidad)
    }
    class Venta {
        -Long id
        -Instant fechaHora
        -BigDecimal total
        +agregar(Producto, int)
    }
    class DetalleVenta {
        -Long id
        -int cantidad
        -BigDecimal precioUnitario
        -BigDecimal subtotal
    }
    class SesionService {
        +exigirUsuario() Usuario
        +exigirAdministrador() Usuario
        +cerrar()
    }
    class AuthService {
        +iniciar(String, String)
        +configurarAdministrador(String, String, String)
    }
    class ClienteService {
        +registrar(Cliente) Cliente
        +actualizar(Long, Cliente) Cliente
        +consultar(Long) Cliente
        +desactivar(Long)
    }
    class ProductoService {
        +registrar(Producto) Producto
        +actualizar(Long, Producto, int stockEsperado) Producto
        +consultar(Long) Producto
        +desactivar(Long)
    }
    class CategoriaService
    class UsuarioService
    class CarritoService {
        +agregar(Producto, int)
        +total() BigDecimal
        +vaciar()
    }
    class VentaService {
        +registrar(List, Long) Venta
        +consultarDia(LocalDate) List
        +totalDia(LocalDate) BigDecimal
    }
    class ComprobanteService {
        +generar(Venta) String
    }
    Categoria "1" <-- "*" Producto
    Usuario "1" <-- "*" Venta
    Cliente "0..1" <-- "*" Venta
    Venta "1" *-- "1..*" DetalleVenta
    Producto "1" <-- "*" DetalleVenta
    AuthService --> SesionService
    ClienteService --> Cliente
    ProductoService --> Producto
    CategoriaService --> Categoria
    UsuarioService --> Usuario
    VentaService --> Venta
    VentaService --> SesionService
    CarritoService --> Producto
    ComprobanteService --> Venta
```

Los seis repositorios extienden `JpaRepository` y se inyectan en los servicios correspondientes. No hay jerarquía artificial de entidades ni repositorio genérico propio. La composición Venta/DetalleVenta representa el agregado transaccional. Una línea de carrito tiene producto, cantidad y precio cotizado: es un valor transitorio necesario para confirmar exactamente lo presentado.

## Diagrama de casos de uso

```mermaid
flowchart LR
    A[Administrador]
    C[Cajero]
    subgraph Sistema de escritorio
        L([Iniciar y cerrar sesión])
        U([Administrar empleados])
        CC([Registrar y consultar clientes])
        EC([Actualizar y desactivar clientes])
        CAT([Administrar categorías])
        CP([Consultar productos y categorías])
        EP([Administrar productos y stock])
        V([Registrar venta en efectivo])
        CV([Consultar ventas y total diario])
        CO([Ver y guardar comprobante interno])
    end
    A --> L
    A --> U
    A --> CC
    A --> EC
    A --> CAT
    A --> CP
    A --> EP
    A --> V
    A --> CV
    A --> CO
    C --> L
    C --> CC
    C --> CP
    C --> V
    C --> CV
    C --> CO
```

Se incluye además el fuente PlantUML de casos de uso para una representación UML con actores y límite del sistema.

## Secuencia de venta

1. El servicio comprueba sesión activa, cliente opcional y líneas del carrito.
2. Ordena los IDs de producto y adquiere bloqueos de escritura en la transacción.
3. Comprueba producto activo, cantidad, stock y coincidencia del precio cotizado.
4. Crea los detalles con sus precios unitarios y subtotales, acumula total y descuenta stock.
5. Persiste el agregado y confirma la transacción. Una excepción revierte todos los cambios.
6. Tras confirmación, vacía el carrito y presenta el comprobante. Un fallo posterior al guardar el comprobante no repite la venta; esta se recupera en consultas.
